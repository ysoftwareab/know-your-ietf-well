ssthresh = max(cwnd/2, 2*MTU)
cwnd = ssthresh

ssthresh = max(cwnd/2, 2*MTU)
cwnd = 1*MTU

ssthresh = max(cwnd/2, 2*MTU)
cwnd = ssthresh
partial_bytes_acked = 0

ssthresh = max(cwnd/2, 2*MTU)
cwnd = 1*MTU
partial_bytes_acked = 0

