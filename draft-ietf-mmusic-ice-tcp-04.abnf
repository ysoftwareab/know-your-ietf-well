local-preference = (2^12)*(transport-pref) +
                      (2^9)*(direction-pref) +
                      (2^0)*(other-pref)

a=setup attribute.  For example, if an agent selects its passive
sock_i = socket()
