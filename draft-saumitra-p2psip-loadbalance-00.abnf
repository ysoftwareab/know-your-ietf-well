delta = delta/2;     // update delta

virt_server_i = vp  - (vp - virt_server_i)/2;

alpha = alpha + 2;

delta = delta*2;     // update delta

alpha = alpha -2;

virt_server_i = vp  - (vp - virt_server_i) * 2;

