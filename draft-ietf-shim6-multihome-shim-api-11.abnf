optval = 1;

len = sizeof(optval);
optval = 1;

len = sizeof(optval);

len = sizeof(pe);

len = sizeof(lc);

optval = 1;

len = sizeof(optval);

optval = 15; /* 15 seconds */

len = sizeof(optval);

optval = 0;

len = sizeof(optval);

