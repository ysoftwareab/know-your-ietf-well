gname = tstr

role = tstr

scope_entry = [ gname , ? ( role / [ 2*role ] ) ]

scope = << [ + scope_entry ] >>

sign_info_req = nil

sign_info_res = [ + sign_info_entry ]

sign_info_entry =
pub_key_enc = int / nil
gname = tstr

id = bstr

role = tstr

comb_role = [ 2*role ]

get_pub_keys = [ [ *(role / comb_role) ], [ *id ] ]

