gid = bstr

role = tstr

scope_entry = [ gid , ? ( role / [ 2*role ] ) ]

scope = << [ + scope_entry ] >>

sign_info_req = nil

sign_info_res = [ + sign_info_entry ]

sign_info_entry =
gid = bstr

pub_key_enc_req = nil

id = bstr

role = tstr

comb_role = [ 2*role ]

get_pub_keys = [ [ *(role / comb_role) ], [ *id ] ] / [ ]

