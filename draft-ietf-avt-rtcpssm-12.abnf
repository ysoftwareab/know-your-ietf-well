FT = Feedback Target
                      RTCP with Unicast Feedback

                          Figure 1. System Architecture.

x=0; [min, min+(max-min)/NDB]
SRBT=2.
   
att-value  = "reflection"
              / "rsi" *(SP rsi-rule]
   
rsi-rule   = processing ":" rtcp-type
             
processing = "aggr" / "forward" / "term" / token ;keep it extensible
   
                      RTCP with Unicast Feedback

rtcp-type  = 3*3DIGIT    ; the RTCP type (192, 193, 202--208)
    
m= line of the media description.
   
ssrc-attr = "a=ssrc:" ssrc-id SP cname [SP role params] CRLF
   
ssrc-id   = integer      ; in the range [0 ... 2^32-1]
   
cname     = byte-string  ; following the syntax conventions for
                            ; CNAME as defined in [1]
   
role      = "media-sender"
             / token
   
params    = SP byte-string
             / ; empty
   
a=ssrc:314159 iptv-sender@example.com media-sender view 1
   
xrange = Max Distribution Value(MaDV) - Min Distribution Value(MnDV)
xrange = MaDV - MnDV
x = MnDV;
   
y = (ydata[i] * factor);
