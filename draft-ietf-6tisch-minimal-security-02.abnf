C=Critical, U=Unsafe, N=NoCacheKey, R=Repeatable

                   Figure 2: Stateless-Proxy CoAP Option

payload = [
       COSE_KeySet,
       ? short_address,
short_address = [
       address : bstr,
       ? lease_asn : bstr,
