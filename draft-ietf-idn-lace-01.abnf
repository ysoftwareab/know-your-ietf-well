lenp = op++;                 // reserve space
lastHigh = high;
op = iCount + 1;
result = iCount-1;
high = iArray[ip++];
result = op;
bits = bits | input[ip++];
val = (bits >> bitCount);
val = bits << (5 - bitCount);
val = fromLetter(val);
bits = bits | val;
