G = (V,E) - A directed connected graph where V is the set of nodes
               and E is the set of edges in the graph.

newdist = D(S(s'), T) + W(e')

Delta = (d - D(y, T))
 
N = B(y, T)
newdist = D(S(e), T) + W(e)
                
