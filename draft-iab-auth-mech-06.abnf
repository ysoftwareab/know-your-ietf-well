STORED = H(PASSWORD || SALT)

RESPONSE = H(STORED || CHALLENGE)

Channel =           ==========================
     TLS
                           |               |
               Client      |               |     Server
                           V               V

    Authentication     --------------------------
Exchange =
