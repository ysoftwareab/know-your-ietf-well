exp-int = (exp-count + 1) * RTT + SYN. 
exp-count = exp-count + 1. 
inc = 1/MTU; 
inc = max(10^(ceil(log10((B-C)*MTU*8))) * Beta/MTU, 1/MTU); 
I = (I * SYN) / (I * inc + SYN). 
    
W = (W * 7 + AS * (SYN + RTT)) / 8. 
 
 
                                 UDT                     October 2003 
 
 
