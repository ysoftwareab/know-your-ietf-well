i = next % (finger_table_size + 1),

delta = delta/2;     // update delta

vs_i = vp  - (vp - vs_i)/2;

alpha = alpha + 2;
delta = delta * 2;     // update delta
alpha = alpha - 2;     // update alpha

vs_i = vp  - (vp - vs_i) * 2;
N = ---------
                                  d


U = ----------,
                                M * Tk


L = --- * ---------------,
                               4     Ages[rsize/4]


Tf = --------
                                 2*U


Tstab-1 = -----------
                                     log2^2(N)


Tstab-2 = ---------------
                                     L * log2^2(N)


