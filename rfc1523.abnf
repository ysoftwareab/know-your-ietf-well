nofill=0;
                     char token[62], *p;

                     while ((c=getc(stdin)) != EOF) {
                         if (c == '<') {
                             if (verbatim != 0) {
                                 for (i=0, p=token; (*p++ = getc(stdin))
verbatim = 0;
newlinect=0;
c = getc(stdin);
verbatim = 1;
newlinect = 0;
