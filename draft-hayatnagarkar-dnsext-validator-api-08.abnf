done = 1;
rc = val_async_submit(ctx, domain, ns_c_in, ns_t_a,
                           VAL_AS_ONE_CALLBACK, my_callback,
                           (void*)domain, &async_status);
nfds = 0;            /* no FDs yet */
ready = select(numfds+1, &fds, NULL, NULL, &tv);
