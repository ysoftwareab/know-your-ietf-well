Host-Uniq = R1 (assigned)

Host-Uniq = R2 (assigned)

Host-Uniq = R1 (echoed)
AC-Cookie = C1 (assigned)

Host-Uniq = R2 (echoed)
AC-Cookie = C2 (assigned)

AC-Cookie = C1' (echoed)

AC-Cookie = C1 (echoed)

Host-Uniq = R1 (assigned)

Host-Uniq =  R2 (assigned)

Host-Uniq = R2 (echoed)
AC-Cookie = C1 (assigned)

Host-Uniq = R1 (echoed)
AC-Cookie = C1' (assigned)

AC-Cookie = C1'' (assigned)

AC-Cookie = C1'' (echoed)

AC-Cookie = C1' (echoed)

AC-Cookie = C1 (echoed)

Host-Uniq = R1 (assigned)

Host-Uniq = R2 (assigned)

Host-Uniq = R2 (echoed)
AC-Cookie = C1 (assigned)

Host-Uniq = R2 (echoed)
AC-Cookie = C2 (assigned)
Host-Uniq = R1 (echoed)
Host-Uniq = R1
AC-Cookie = C2'' (echoed)

AC-Cookie = C1 (echoed)

