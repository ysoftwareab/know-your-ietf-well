Type=Domain:

Type=Referral:

Type=Domain/Referral/SingleEntry/Default.

Type=Individual:

QType=SEN(1):  QData=PI address that needs to be resolved.

type=SEN, ANCOUNT=1, NSCOUNT=1, ARCOUNT= Number of levels

Type=SOA: PI address of server+SERIAL+REFRESH+RETRY+EXPIRE+MINIMUM

Type=(SEN/MPS): NAddr(Number of addresses) + corresponding PA
Type=(DMN/DEF): NAddr(Number of addresses) + corresponding Net
Type=IND: NAddr(=1) + full path name of the data file

Type=REF: NAddr(Number of map server) + for each map server (PI
