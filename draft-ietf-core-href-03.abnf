CRI-Reference = [
        (?scheme, ?((host.name // host.ip), ?port) // path.type),
        *path,
        *query,
        ?fragment
scheme    = (0, text .regexp "[a-z][a-z0-9+.-]*")
port      = (3, 0..65535)
path      = (5, text)
query     = (6, text)
fragment  = (7, text)

