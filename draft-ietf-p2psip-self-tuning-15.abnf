numBitsInNodeId=128 bits.  The identifiers are ordered on an
N = -------------------
                                d

U = --------,
                          M * Tk

L = ----------------------,
                         Ages[floor(rsize/2)]

Tf = ------
                                 2*U

Tstab-1 = -----------------
                                      square(log2(N))

Tstab-2 = ---------------------
                                       L * square(log2(N))

