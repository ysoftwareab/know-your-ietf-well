X_supp = (1-g) * X_exp

Rs_ins = t_now - ts_i

R_max = Rs_ins

R_max = MAX(R_max * 0.9, R_peak)

R_sample = t_now - ts_r

R = R_sample
R = q*R + (1-q)*R_sample

r = cwnd * packet_size / R

R_tot = 0;
W_tot = 0;
R_tot = R_tot + (r(i) * w(i));
W_tot = W_tot + w(i);
X_exp = R_tot/W_tot;
                      Expires: January 17, 2006            July 17, 2005


t = max(T * (1 + log(x)/log(N)), 0)

cwnd = X_send * RTT_max

cwnd = X_exp * R

