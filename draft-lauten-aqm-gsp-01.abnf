interval = e.g. 2 * RTT
threshold = e.g. 1/2 * buffer size
timeout_expiry = now(), with now() returning the current time

timeout_expiry = now() + interval
tau = e.g. 2 seconds; a preset parameter controlling the
           adaptation speed
cumTime = 0; the cumulative time above/below threshold

cumTime = max(cumTime, -tau)
cumTime = min(cumTime, some sanity limit of several minutes)

k = max(1, cumTime/tau)
interval = initial_interval/k

timeout_expiry = now() + interval



queue_delay = now() - p.time

timeout_expiry = now() + interval
