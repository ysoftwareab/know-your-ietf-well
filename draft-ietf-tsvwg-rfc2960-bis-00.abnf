Strm=0,Seq=1 & user data]--\
Block=0]     --------\  /        [TSN=init TSN_Z +1,
                               \/          Strm=0,Seq=2 & user data 2]
                        <------/\
                                 \
                                  \------>

                     Figure 4: INITiation Example

Tag=Tag_A                                             Tag=Tag_Z
Tie=Tag_A,
Strm=0,Seq=1 & user data]--\
ssthresh = max(cwnd/2, 2*MTU)
cwnd = ssthresh

ssthresh = max(cwnd/2, 2*MTU)
cwnd = 1*MTU

inp = &buf[8];
insum = *inp;
sum = fletcher(buf, length);
nlen = len / 2;

bufp = (unsigned short *)buf;
s1 = ((s1 + *bufp) & 0x0000ffff);
s2 = ((s2 + s1) & 0x0000ffff);
x = buf[(len-1)] << 8 | 0;
s1 = ((s1 + x) & 0x0000ffff);
s2 = ((s2 + s1) & 0x0000ffff);
