host          = hostname | IPv4address | IPv6reference
ipv6reference = "[" IPv6address "]"

reserved    = ";" | "/" | "?" | ":" | "@" | "&" | "=" | "+" |
                     "$" | "," | "[" | "]"

unwise      = "{" | "}" | "|" | "\" | "^" | "`"

